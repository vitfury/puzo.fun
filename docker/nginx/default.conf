server {
    listen 80;
    listen [::]:80;
    server_name melody.ninja www.melody.ninja localhost;

    root /var/www/html/public;
    index index.php index.html;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/json application/javascript;

    # Client max body size (for file uploads)
    client_max_body_size 50M;

    # Logs
    access_log /var/log/nginx/melody_ninja_access.log;
    error_log /var/log/nginx/melody_ninja_error.log;

    # API routes
    location /api {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # Admin routes
    location /admin {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # Frontend static files
    location /frontend {
        alias /var/www/html/public/frontend;
        try_files $uri $uri/ /frontend/index.html;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Storage files
    location /storage {
        alias /var/www/html/storage/app/public;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # PHP-FPM configuration
    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass app:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
        fastcgi_read_timeout 300;
    }

    # Deny access to hidden files
    location ~ /\. {
        deny all;
    }

    # Deny access to sensitive files
    location ~ /\.(?!well-known).* {
        deny all;
    }

    # Default location
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }
}

# HTTPS configuration (uncomment when SSL certificates are ready)
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name melody.ninja www.melody.ninja;
#
#     ssl_certificate /etc/nginx/ssl/melody.ninja.crt;
#     ssl_certificate_key /etc/nginx/ssl/melody.ninja.key;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     # Same configuration as above...
#     root /var/www/html/public;
#     index index.php index.html;
#
#     # ... (copy all locations from HTTP block)
# }

# Redirect HTTP to HTTPS (uncomment when SSL is ready)
# server {
#     listen 80;
#     listen [::]:80;
#     server_name melody.ninja www.melody.ninja;
#     return 301 https://$server_name$request_uri;
# }
