user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;

    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;
    client_max_body_size 20M;

    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml text/javascript
               application/json application/javascript application/xml+rss
               application/rss+xml font/truetype font/opentype
               application/vnd.ms-fontobject image/svg+xml;

    # HTTP to HTTPS redirect!!
    server {
        listen 80;
        server_name puzo.fun www.puzo.fun;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }

    # HTTPS server
    server {
        listen 443 ssl http2;
        server_name puzo.fun www.puzo.fun;

        # SSL configuration
        ssl_certificate /etc/letsencrypt/live/puzo.fun/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/puzo.fun/privkey.pem;

        # SSL security settings
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384';
        ssl_prefer_server_ciphers off;
        ssl_session_cache shared:SSL:10m;
        ssl_session_timeout 10m;

        # Security headers
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;

        root /var/www/html;
        index index.html;

        # API routes
        location /api/ {
            try_files $uri $uri/ /index.php?$query_string;
        }

        # PHP-FPM for Laravel
        location ~ \.php$ {
            fastcgi_pass php:9000;
            fastcgi_index index.php;
            fastcgi_param SCRIPT_FILENAME /var/www/html/public$fastcgi_script_name;
            include fastcgi_params;
            fastcgi_read_timeout 300;
        }

        # Storage files
        location /storage/ {
            alias /var/www/html/storage/;
            expires 30d;
            add_header Cache-Control "public, immutable";
        }

        # No-cache for index.html to prevent caching issues
        location = /index.html {
            add_header Cache-Control "no-cache, no-store, must-revalidate";
            add_header Pragma "no-cache";
            add_header Expires "0";
        }

        # Cache hashed JS and CSS files (Vite generates files with content hashes)
        # These files can be cached forever because their names change when content changes
        location ~* \.(js|css)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
            access_log off;
        }

        # Cache static assets (images, fonts, etc.)
        location ~* \.(jpg|jpeg|png|gif|ico|svg|woff|woff2|ttf|eot|webp|avif)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
            access_log off;
        }

        # Cache manifest and service worker files
        location ~* \.(webmanifest|json)$ {
            expires 1d;
            add_header Cache-Control "public, must-revalidate";
        }

        # Static files from React build (fallback for SPA routing)
        # This should NOT cache anything, just serve files and fallback to index.html
        location / {
            try_files $uri $uri/ /index.html;
            # Don't set cache headers here - let specific rules above handle it
        }

        # Deny access to hidden files
        location ~ /\. {
            deny all;
        }
    }
}
